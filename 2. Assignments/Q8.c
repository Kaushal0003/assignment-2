#include <stdio.h>
int main()
{
    printf("Size of an integer is %d bits\n", sizeof(int));
    printf("Size of an float is %d bits\n", sizeof(float));
    printf("Size of an char is %d bits\n", sizeof(char));
    printf("Size of an double is %d bits\n", sizeof(double));
    return 0;
}